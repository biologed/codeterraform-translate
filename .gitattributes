# Единый перевод строк в репозитории; в рабочей копии Windows-инструменты
# получат CRLF сами.
* text=auto eol=lf

# У .bat перевод строк должен остаться windows-овым, иначе cmd.exe спотыкается.
*.bat text eol=crlf
